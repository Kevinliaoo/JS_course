// Datatypes and variables

// Variable: A piece of data that is stored in the computer
let age; // I created a variable called "age". Define a variable

// Assign a value to my variable
age = 90; // Assign a value to a variable

// Comments are not "Visible" form Js

// Re assign a value (change the value of the variable)
age = 15;

/**
 * Naming the variables
 *
 * We can name a variable as we want, BUT
 * Variable's names can contain letters, numbers, _ or $
 * The names CAN NOT start with a number
 * The variables names MUST be compounded of one single word
 * We can not have two varriables with the same name
 * We CAN NOT use spetial words (reserved words)
 *
 * Cammel case
 * let name;
 * let nameOfTheUser;
 * let dateOfBirth;
 * We separate the words using an uppercase instead of a space
 *
 * Snake case (?)
 * let name_of_the_user;
 * let date_of_brith;
 *
 * let, var, const, number, function, if, else, switch, class
 */

// Inicialize a variable
let name;
// Assign a value
name = 'Kevin';

// Initialize and assign a value to a variable
let birthday = '19 feb';

// Datatypes

// numbers
let year = 2021; //  integer
let temperature = -39.0; // negative

let time = 9.9; // float
let weight = 70.5;

// strings (word/phrase)
let myName = 'Kevin';
let myBirthday = '19 february of 2002';

let height = '174';
let height2 = 174;

// boolean (true / false)
// Booleans have only two values
let amITall = false;
let didIPassTheExam = true;
